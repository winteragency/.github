<!-- Written in English. As short as the change allows, never more than 400 words. Use only the headings below, in this order. No sub-headings, no diff walkthrough, no session narration, no footers. -->

## What

<!-- One to three sentences. The change as a reader outside the team would understand it. -->

**Task:** <!-- For a GitHub issue, use a closing keyword so GitHub links and closes it: "Closes #12", "Fixes #12", or "Refs #12" when this PR does not finish it. For Asana or Zendesk, a plain link. Delete this line if there is no task. -->

## Why

<!-- The problem or request, in enough words that this still reads when the task link is dead. Only if a reviewer would ask "why not X": one sentence on why not. -->

## Verification

<!-- For UI: a before/after screenshot. For behaviour: the exact steps you ran and what you saw. Name new tests by what they cover, not how many. CI reports the checks, so no test counts or "lint green". Say what was not tested and why. -->

## Notes for the reviewer

<!-- Optional. Delete if empty. Where to start, the risky part, and what was deliberately left out. -->

## After merge

<!-- Optional. Delete unless a person must do something by hand after merge: set a secret, change config on a server, re-provision, tell the customer. Anything the deploy does on its own, such as running migrations, does not belong here. -->
